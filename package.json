{
  "name": "claros-genai-bundle",
  "private": true,
  "description": "One-time build of @google/genai for frontend; not used at runtime.",
  "scripts": {
    "build:genai": "node build-genai.mjs",
    "test:session-rules": "node tests/session-rules.test.cjs"
  },
  "devDependencies": {
    "@google/genai": "^1.45.0",
    "esbuild": "^0.24.0"
  }
}
